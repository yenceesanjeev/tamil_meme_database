import React from "react";
import { Input } from "@/components/ui/input";

const SearchBar = () => {
  return (
    <div className="min-w-[300px] items-center justify-center">
      <Input placeholder="Search for Tamil Memes" />
    </div>
  );
};

export default SearchBar;
